#include "variables_ambito_intermedio.h"
#include <stdio.h>

void imprimirMensaje() {
    int local = 10;  // Variable local al bloque de la función
    printf("Valor de la variable local: %d\n", local);
}